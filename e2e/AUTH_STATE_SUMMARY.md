# 認証状態の修正完了

## ✅ 修正内容

全ての認証が必要なテストファイルを修正し、**保存された認証状態**を使用するようにしました。

### 修正前

```typescript
test.beforeEach(async ({ page }) => {
  // サインイン処理（2段階認証で止まる）
  await page.goto('/ja/signin');
  await page.fill('[name="email"]', testEmail);
  await page.fill('[name="password"]', testPassword);
  await page.click('button[type="submit"]');
  await page.waitForURL(/\/ja\/account/, { timeout: 10000 });
});
```

### 修正後

```typescript
// 保存された認証状態を使用
test.use({ storageState: 'playwright/.auth/user.json' });

test.beforeEach(async ({ page }) => {
  // 認証済み状態で直接ページへ移動
  await page.goto('/ja/account/user');
});
```

---

## 📂 修正したファイル（15ファイル）

1. ✅ `e2e/service.spec.ts`
2. ✅ `e2e/navigation.spec.ts`
3. ✅ `e2e/api-management.spec.ts`
4. ✅ `e2e/user-management.spec.ts`
5. ✅ `e2e/group-management.spec.ts`
6. ✅ `e2e/policy-management.spec.ts`
7. ✅ `e2e/payment.spec.ts`
8. ✅ `e2e/api-keys.spec.ts`
9. ✅ `e2e/usage-limit.spec.ts`
10. ✅ `e2e/audit-log.spec.ts`
11. ✅ `e2e/company-info.spec.ts`
12. ✅ `e2e/account-deletion.spec.ts`
13. ✅ `e2e/support.spec.ts`
14. ✅ `e2e/announcement.spec.ts`
15. ✅ `e2e/new-order.spec.ts`
16. ✅ `e2e/group.spec.ts`
17. ✅ `e2e/responsive.spec.ts`（認証済みページのテストのみ）

---

## 🎯 メリット

### 1. 2段階認証を回避
- ✅ サインインフローをスキップ
- ✅ メール確認不要
- ✅ 認証コード入力不要

### 2. テスト実行が高速化
- ✅ サインイン処理（約10秒）をスキップ
- ✅ 各テストが即座に開始
- ✅ 全体で数分の短縮

### 3. メンテナンスが容易
- ✅ サインイン処理の重複を削除
- ✅ 認証状態を一元管理
- ✅ テストコードがシンプルに

---

## 🚀 使い方

### ステップ1: 初回のみ手動サインイン

```bash
npx playwright test
```

- ブラウザが開く
- サインインページが表示される
- **手動でサインイン**（メール + パスワード + 2段階認証）
- 認証状態が `playwright/.auth/user.json` に保存される

### ステップ2: 2回目以降は自動

```bash
npx playwright test
```

- 保存された認証状態を使用
- サインインをスキップ
- 全テストが認証済み状態で実行

---

## 🔄 認証状態のリセット

再度サインインする場合:

```bash
# Windows
del playwright\.auth\user.json

# Mac/Linux
rm playwright/.auth/user.json

# 次回のテスト実行時に再度手動サインイン
npx playwright test
```

---

## 📊 テストの分類

### 認証不要テスト（認証状態を使用しない）

- `e2e/signin.spec.ts` - サインインページ
- `e2e/signup.spec.ts` - サインアップページ
- `e2e/public-pages.spec.ts` - 公開ページ
- `e2e/navigation.spec.ts` - 未認証ユーザーのナビゲーション
- `e2e/responsive.spec.ts` - 公開ページのレスポンシブデザイン
- `e2e/accessibility.spec.ts` - アクセシビリティ
- `e2e/performance.spec.ts` - パフォーマンス
- `e2e/error-handling.spec.ts` - エラーハンドリング
- `e2e/flow.spec.ts` - フロー

### 認証必要テスト（認証状態を使用）

上記の15ファイル + `e2e/navigation.spec.ts`（認証済みユーザーのテスト）

---

## ✅ 次のステップ

1. **初回テスト実行** - 手動サインインを完了
   ```bash
   npx playwright test
   ```

2. **認証状態の確認** - ファイルが作成されたか確認
   ```bash
   # Windows
   dir playwright\.auth\user.json
   
   # Mac/Linux
   ls -la playwright/.auth/user.json
   ```

3. **2回目以降のテスト** - 自動的に認証状態を使用
   ```bash
   npx playwright test
   ```

---

## 🎉 完了！

これで、認証が必要なページのテストが正常に実行されるようになりました。

